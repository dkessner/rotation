{
  "name": "ics_transform",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "assert": "^2.0.0",
    "fs": "^0.0.1-security",
    "ical.js": "^1.4.0"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.15.0",
    "babel-core": "^6.26.3",
    "babelify": "^10.0.0"
  },
  "browserify": {
    "transform": [
      [
        "babelify",
        {
          "presets": [
            "@babel/preset-env"
          ]
        }
      ]
    ]
  }
}
